# Build outputs (regenerated by build.bat) — do not commit.
*.hdv
main.bin
build/

# Ignore floppy images generally, but keep this conceptual .po in git (the HDV is the real one).
*.po
!TimePilot-IIvera.po
